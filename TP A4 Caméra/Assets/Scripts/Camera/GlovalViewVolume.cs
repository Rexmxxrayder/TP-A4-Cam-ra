using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GlovalViewVolume : AViewVolume
{
    // Start is called before the first frame update
    void Start()
    {
        SetActive(true);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
